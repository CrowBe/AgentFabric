{
  "agentsop": "0.1",
  "id": "workspace.discover",
  "title": "Discover workspace resources",
  "description": "List fabric-managed resources in the workspace. This is a ref-introducing operation: it issues ResourceRefs for files the fabric is willing to expose, which is how those resources enter the caller's reachable world. Knowing an implementation locator does not produce a ref. Does not accept locators and does not search outside the workspace.",
  "input": {
    "type": "object",
    "properties": {
      "kind": {
        "type": "string",
        "description": "If set, only return resources of this kind."
      }
    },
    "additionalProperties": false
  },
  "output": {
    "type": "object",
    "properties": {
      "resources": {
        "type": "array",
        "items": {
          "type": "object",
          "additionalProperties": false,
          "required": ["resource", "label"],
          "properties": {
            "resource": { "$ref": "#/$defs/ResourceRef" },
            "label": { "type": "string" }
          }
        }
      }
    },
    "required": ["resources"],
    "additionalProperties": false
  },
  "effects": ["discover"],
  "idempotent": true,
  "authority": {
    "resources": [],
    "effects": ["discover"]
  },
  "depends_on": []
}
